package service;

import entity.User;
import repository.UserRepository;
import specification.searchSpecification.FindByLoginAndPassword;

import java.util.Optional;

public class UserService {

 /*   public Optional<User> login(String login, String page) {
        UserRepository userRepository = new UserRepository();
        FindByLoginAndPassword findByLoginAndPassword = new FindByLoginAndPassword();

        return userRepository.query(findByLoginAndPassword);
    }*/

}
